spring:
  datasource:
    url: jdbc:h2:file:./build/db/dev-cloud-storage;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
